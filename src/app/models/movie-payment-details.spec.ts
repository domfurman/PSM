import { MoviePaymentDetails } from './movie-payment-details';

describe('MoviePaymentDetails', () => {
  it('should create an instance', () => {
    expect(new MoviePaymentDetails()).toBeTruthy();
  });
});
